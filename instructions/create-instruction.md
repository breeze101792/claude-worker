# Create Instruction or Inspiration

Trigger: User asks to "create instruction", "add SOP", "add inspiration", or similar.

Action:
1. Create the file in `instructions/` or `inspiration/` directory
2. Update `INSTRUCTIONS.md` or `INSPIRATION.md` — add entry to the table with filename and purpose
3. Use clear, concise language
