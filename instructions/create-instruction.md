# Create Instruction

Trigger: User asks to "create instruction", "add SOP", or similar.

Action:
1. Create the instruction file in `instructions/` directory
2. Update `INSTRUCTIONS.md` — add entry to the table with filename and purpose
3. Use clear, concise language in the instruction
